def main():
    SIZE = 5
    numbers = [0, 0, 0, 0, 0]
    index = int(); value = int(20); total = int()
    for index in range(0, SIZE):
        numbers[index] = value
        value = value + 10
        total = total + numbers[index]
main()
